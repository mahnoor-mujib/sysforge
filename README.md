# sysforge
SysForge is a Windows desktop app with three real-time tabs running concurrently via Windows APIs. Background threading ensures all operations (monitoring, scanning, processes) run without blocking the GUI, delivering a fast and fully responsive user experience.
